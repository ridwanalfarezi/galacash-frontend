# Browser requests remain same-origin and are sent through /api.
VITE_API_URL=/api

# Server-side proxy destination. Set this in the frontend Vercel project after
# deploying galacash-server, without a trailing /api path.
API_URL=https://YOUR_BACKEND_PROJECT.vercel.app
